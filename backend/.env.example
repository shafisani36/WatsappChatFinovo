NODE_ENV=development

DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=task_manager
DB_USER=postgres
DB_PASSWORD=12345

PORT=5000
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES=1d

# Comma-separated if you need more than one (e.g. local dev + Vercel preview + production)
CLIENT_URL=http://localhost:5173

